type Query {
  id: String
}

type Mutation {
  createBook(book: BookCreateInput): Book
}

type Book {
  _id: ID!
  ISBN: String!
  imageURI: String!
  title: String!
  description: String!
  status: BookStatus!
  authors: [Author!]!
  publisher: Publishers
  publishedDate: String!
  genre: [Genre!]!
  borrowerId: ID
  borrowDate: String
  dueDate: String
}

enum Publishers {
  Harper_Collins
}

enum Genre {
  Fantasy
}

enum BookStatus {
  Available
  Unavailable
}

input BookCreateInput {
  ISBN: String!
  imageURI: String!
  title: String!
  description: String!
  status: BookStatus!
  authors: [ID!]!
  publisher: Publishers
  publishedDate: String!
  genre: [Genre!]
}
