type Mutation {
  createAuthor(author: AuthorCreateInput): Author
  deleteAuthor(authorId: ID): Author
}

input AuthorCreateInput {
  name: String!
}

type Author {
  _id: ID!
  name: String!
  books: [Book]!
}
